import type { Value } from 'node-pg-migrate/dist/operations/generalTypes';
export interface DomainOptions {
    default?: Value;
    notNull?: boolean;
    check?: string;
    constraintName?: string;
}
