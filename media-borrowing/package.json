{
  "name": "media-borrowing",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./build && npx tsc",
    "start": "npm run build && npx functions-framework --source=build/ --target=myHttpFunction"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@google-cloud/functions-framework": "^3.4.2",
    "@types/node": "^22.9.0",
    "pnpm": "^9.12.3",
    "rimraf": "^6.0.1",
    "typescript": "^5.6.3"
  }
}
