import type { MigrationOptions } from 'node-pg-migrate/dist/types';
import type { Name, Reversible } from 'node-pg-migrate/dist/operations/generalTypes';
import type { DropOperatorOptions } from 'node-pg-migrate/dist/operations/operators/dropOperator';
export interface CreateOperatorOptions {
    procedure: Name;
    left?: Name;
    right?: Name;
    commutator?: Name;
    negator?: Name;
    restrict?: Name;
    join?: Name;
    hashes?: boolean;
    merges?: boolean;
}
export type CreateOperatorFn = (operatorName: Name, operatorOptions: CreateOperatorOptions & DropOperatorOptions) => string;
export type CreateOperator = Reversible<CreateOperatorFn>;
export declare function createOperator(mOptions: MigrationOptions): CreateOperator;
